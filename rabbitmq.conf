# RabbitMQ Stream Configuration for Docker
# This configures the advertised host/port for stream protocol

# Stream plugin configuration
stream.advertised_host = localhost
stream.advertised_port = 5552

